# Messing around with countries and kotlin

Run tests: 
```./gradlew test```

Run app:
```./gradlew run```

Run with custom sort:
```./gradlew run --args XXX```

where XXX is `population|area`, default is alphabetical by country name.
